<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="Company search" />
    <title>Company search</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag :application %>
  </head>

  <body>
    <!-- Navigation-->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container px-4 px-lg-5">
      </div>
    </nav>
    <!-- Header-->
    <header class="bg-dark py-5">
      <div class="container px-4 px-lg-5 my-5">
        <div class="text-center text-white">
          <h1 class="display-4 fw-bolder">Search for your favorite companies</h1>
          <div class="row">
            <div class="col-lg-8 justify-content-center align-items-center m-auto">
              <form action="/" method="get">
                <div class="form-group mx-sm-3 mb-2">
                  <div class="row">
                    <div class="col-10">
                      <input type="text" class="form-control" id="search_string" name="search_string" placeholder="search string" value="<%= params[:search_string] %>">
                    </div>
                    <div class="col-2">
                      <button type="submit" class="btn btn-primary mb-2">Search</button>
                    </div>
                  </div>
                  <div class="row">
                    <div class="col-10 align-text-top" style="text-align: justify; margin-top: -10px;">
                      <% if flash[:notice] %>
                        <%= flash[:notice] %>
                      <% end %>
                    </div>
                    <div class="col-2"></div>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </header>
  <%= yield %>
  </body>
</html>
